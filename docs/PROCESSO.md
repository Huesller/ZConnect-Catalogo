# Processo

## Fluxo diário

```bash
git pull
npm run build
git add .
git commit -m "Mensagem"
git push origin main
```

## Com DevKit

Execute:

```txt
ZCONNECT.bat
```

ou diretamente:

```txt
dev/PUBLICAR.bat
```
